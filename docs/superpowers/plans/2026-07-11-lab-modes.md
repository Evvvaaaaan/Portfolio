# Lab Modes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 포트폴리오 메인 페이지(`/`)에 옵트인 테마 모드 4종(Terminal, Speedrun, Destruction, Inspect)을 덧입히는 Lab Modes 시스템 구현.

**Architecture:** `src/modes/`에 experiments 레지스트리와 같은 패턴의 모드 레지스트리를 두고, 각 모드는 lazy 오버레이 컴포넌트로 격리한다. `ModeProvider`(React context)가 활성 모드 상태의 단일 소스이며, `ModeLayer`가 메인 페이지에서만 활성 모드를 Suspense + ErrorBoundary 안에 렌더한다. 기존 섹션 코드는 Projects의 데이터 추출(1곳) 외에 수정하지 않는다.

**Tech Stack:** React 19, react-router-dom 7, matter-js 0.20(기존 의존성), Vitest(신규 devDep — 순수 로직 단위 테스트), Playwright(devDep 있음, 설정 신규 — E2E).

**Spec:** `docs/superpowers/specs/2026-07-11-lab-modes-design.md` — 이 계획의 요구사항 원본.

## Global Constraints

- **핵심 제약: 모드는 포트폴리오 본래 메시지 전달을 방해하면 안 된다.** 자동 활성화 금지, 100% 옵트인, normal 상태의 메인 페이지는 렌더 트리·성능·접근성 모두 도입 전과 동일.
- 모드 컴포넌트 코드는 전부 `lazy()` — normal 상태에서 로드되지 않아야 함.
- 적용 범위는 메인 페이지(`/`)만. 모드 상태는 세션 내 메모리에만 (URL·storage 반영 금지, 예외: speedrun 최고기록 `localStorage` 키 `labmode.speedrun.best`).
- ESC 키는 어떤 모드에서든 즉시 normal 복귀. 모드 크래시 시 ErrorBoundary가 normal로 강제 복귀.
- 모든 모드 컴포넌트는 언마운트 시 자기 부작용(DOM 변경, 리스너, rAF, Matter 엔진, Lenis 정지)을 완전히 원복.
- 모드 UI 카피는 영어 고정. Terminal이 출력하는 콘텐츠는 `useLang()`의 현재 언어 데이터를 재사용.
- Destruction 물리 바디 상한 80개 (`MAX_BODIES`).
- z-index 규약: 모드 오버레이 2000, 모드 뱃지 2100 (Navbar 위).
- 테스트 명령: 단위 `npm test` (vitest), E2E `npm run test:e2e` (playwright, chromium만).
- 커밋 메시지 끝에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- 작업 트리에 이 기능과 무관한 사용자 미커밋 변경이 있음 (`src/experiments/`, `Gallery.css`, `ProjectPage.jsx`, `Projects.css`, `Projects.jsx`). **절대 스테이징·되돌리기 금지.** 커밋 시 이 계획이 만든/수정한 파일만 `git add`.

---

### Task 1: Mode registry + ModeContext + Vitest 기반

**Files:**
- Create: `src/modes/registry.js`
- Create: `src/modes/ModeContext.jsx`
- Create: `src/modes/registry.test.js`
- Create: `vitest.config.js`
- Modify: `package.json` (devDep `vitest`, script `"test"`)

**Interfaces:**
- Produces: `modes` 배열 (각 항목 `{ id, title, description, color, component }`), `getMode(id) → mode | null`, `validateModes(list) → string[]`, `<ModeProvider>`, `useMode() → { modeId, setModeId, exitMode }`. 이후 모든 태스크가 이를 소비한다.

- [ ] **Step 1: vitest 설치 + 스크립트**

```bash
npm i -D vitest
```

`package.json`의 scripts에 추가 (기존 스크립트 유지):

```json
"test": "vitest run"
```

- [ ] **Step 2: vitest 설정 작성**

`vitest.config.js`:

```js
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    include: ['src/**/*.test.{js,jsx}'],
    environment: 'node',
  },
})
```

- [ ] **Step 3: 실패하는 테스트 작성**

`src/modes/registry.test.js`:

```js
import { describe, it, expect } from 'vitest'
import { modes, getMode, validateModes } from './registry.js'

describe('mode registry', () => {
  it('has no validation errors', () => {
    expect(validateModes(modes)).toEqual([])
  })

  it('getMode returns the mode for a registered id and null otherwise', () => {
    for (const m of modes) expect(getMode(m.id)).toBe(m)
    expect(getMode('nope')).toBeNull()
  })

  it('validateModes reports duplicates and missing fields', () => {
    const ok = { id: 'a', title: 'A', description: 'a', color: '#fff', component: () => null }
    const errors = validateModes([ok, { ...ok, title: '' }])
    expect(errors).toContain('duplicate id: a')
    expect(errors).toContain('a: missing title')
  })
})
```

- [ ] **Step 4: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL — `Cannot find module './registry.js'` 류의 로드 에러.

- [ ] **Step 5: registry 구현**

`src/modes/registry.js`:

```js
// Lab Modes 레지스트리. 각 모드 구현 태스크에서 modes 배열에 항목을 추가한다.
// 항목 형태: { id, title, description, color, component(lazy) }
export const modes = []

export function getMode(id) {
  return modes.find((m) => m.id === id) ?? null
}

export function validateModes(list) {
  const errors = []
  const seen = new Set()
  for (const m of list) {
    for (const key of ['id', 'title', 'description', 'color', 'component']) {
      if (!m[key]) errors.push(`${m.id ?? '?'}: missing ${key}`)
    }
    if (seen.has(m.id)) errors.push(`duplicate id: ${m.id}`)
    seen.add(m.id)
  }
  return errors
}
```

- [ ] **Step 6: ModeContext 구현**

`src/modes/ModeContext.jsx`:

```jsx
import { createContext, useCallback, useContext, useMemo, useState } from 'react'

const ModeContext = createContext(null)

export function ModeProvider({ children }) {
  const [modeId, setModeId] = useState(null)
  const exitMode = useCallback(() => setModeId(null), [])
  const value = useMemo(() => ({ modeId, setModeId, exitMode }), [modeId, exitMode])
  return <ModeContext.Provider value={value}>{children}</ModeContext.Provider>
}

export function useMode() {
  return useContext(ModeContext)
}
```

- [ ] **Step 7: 테스트 통과 확인**

Run: `npm test`
Expected: PASS — 3 passed.

- [ ] **Step 8: Commit**

```bash
git add src/modes/registry.js src/modes/ModeContext.jsx src/modes/registry.test.js vitest.config.js package.json package-lock.json
git commit -m "feat(modes): add mode registry and ModeContext with vitest setup

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: ModeMenu(Navbar 진입점) + ModeLayer + App 통합 + Playwright 기반

**Files:**
- Create: `src/modes/ModeSelector/ModeMenu.jsx`
- Create: `src/modes/ModeSelector/ModeMenu.css`
- Create: `src/modes/ModeLayer.jsx`
- Create: `src/modes/ModeErrorBoundary.jsx`
- Create: `src/modes/modes.css`
- Modify: `src/App.jsx` (AppContent에 ModeProvider 래핑 + ModeLayer 렌더)
- Modify: `src/components/Navbar/Navbar.jsx` (데스크톱 nav-links에 ModeMenu 1줄)
- Create: `playwright.config.js`
- Create: `e2e/mode-menu.spec.js`
- Modify: `package.json` (script `"test:e2e"`)

**Interfaces:**
- Consumes: Task 1의 `modes`, `getMode`, `ModeProvider`, `useMode`.
- Produces: `<ModeMenu />` (Navbar 안 진입점), `<ModeLayer />` (활성 모드 렌더 + ESC + 뱃지). 이후 모드 태스크는 registry 등록만 하면 UI에 나타난다.

- [ ] **Step 1: Playwright 설정 + 스크립트**

`playwright.config.js`:

```js
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: 'e2e',
  timeout: 30_000,
  use: { baseURL: 'http://localhost:5173' },
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: true,
  },
})
```

`package.json` scripts에 추가: `"test:e2e": "playwright test"`

크로미움이 없으면: `npx playwright install chromium`

- [ ] **Step 2: 실패하는 E2E 작성**

`e2e/mode-menu.spec.js`:

```js
import { test, expect } from '@playwright/test'

test('mode menu appears on main page only and opens a panel', async ({ page }) => {
  await page.goto('/')
  const btn = page.getByRole('button', { name: 'Mode' })
  await expect(btn).toBeVisible()
  await btn.click()
  await expect(page.getByRole('menuitem', { name: /Normal/ })).toBeVisible()

  await page.goto('/gallery')
  await expect(page.getByRole('button', { name: 'Mode' })).toHaveCount(0)
})

test('normal page renders unchanged without interacting with modes', async ({ page }) => {
  await page.goto('/')
  await expect(page.locator('#home')).toBeVisible()
  await expect(page.locator('.mode-badge')).toHaveCount(0)
})
```

- [ ] **Step 3: E2E 실패 확인**

Run: `npm run test:e2e`
Expected: FAIL — `getByRole('button', { name: 'Mode' })` not found.

- [ ] **Step 4: ModeMenu 구현**

`src/modes/ModeSelector/ModeMenu.jsx`:

```jsx
import { useEffect, useRef, useState } from 'react'
import { useMode } from '../ModeContext.jsx'
import { modes } from '../registry.js'
import './ModeMenu.css'

export default function ModeMenu() {
  const { modeId, setModeId, exitMode } = useMode()
  const [open, setOpen] = useState(false)
  const rootRef = useRef(null)

  useEffect(() => {
    if (!open) return
    const onDown = (e) => {
      if (!rootRef.current?.contains(e.target)) setOpen(false)
    }
    window.addEventListener('pointerdown', onDown)
    return () => window.removeEventListener('pointerdown', onDown)
  }, [open])

  const pick = (id) => {
    setOpen(false)
    if (id === null) exitMode()
    else setModeId(id)
  }

  return (
    <div className="mode-menu" ref={rootRef}>
      <button
        type="button"
        className={`nav-link mode-menu-btn ${modeId ? 'mode-menu-btn--active' : ''}`}
        aria-haspopup="menu"
        aria-expanded={open}
        onClick={() => setOpen((v) => !v)}
      >
        Mode
      </button>
      {open && (
        <div className="mode-menu-panel" role="menu" aria-label="Lab modes">
          <button type="button" role="menuitem"
            className={`mode-menu-item ${modeId === null ? 'mode-menu-item--on' : ''}`}
            onClick={() => pick(null)}>
            <strong>Normal</strong>
            <span>The portfolio, as intended.</span>
          </button>
          {modes.map((m) => (
            <button key={m.id} type="button" role="menuitem"
              className={`mode-menu-item ${modeId === m.id ? 'mode-menu-item--on' : ''}`}
              style={{ '--mode-color': m.color }}
              onClick={() => pick(m.id)}>
              <strong>{m.title}</strong>
              <span>{m.description}</span>
            </button>
          ))}
        </div>
      )}
    </div>
  )
}
```

`src/modes/ModeSelector/ModeMenu.css`:

```css
.mode-menu { position: relative; }

.mode-menu-btn--active { color: var(--mode-color, #7aa2ff); }

.mode-menu-panel {
  position: absolute;
  top: calc(100% + 14px);
  right: 0;
  min-width: 260px;
  display: flex;
  flex-direction: column;
  gap: 2px;
  padding: 6px;
  border-radius: 12px;
  background: rgba(12, 14, 22, 0.92);
  border: 1px solid rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(12px);
  z-index: 2100;
}

.mode-menu-item {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 2px;
  padding: 10px 12px;
  border: 0;
  border-radius: 8px;
  background: transparent;
  color: inherit;
  text-align: left;
  cursor: pointer;
  font: inherit;
}

.mode-menu-item:hover { background: rgba(255, 255, 255, 0.06); }
.mode-menu-item--on { background: rgba(255, 255, 255, 0.1); }
.mode-menu-item strong { font-size: 0.85rem; color: var(--mode-color, #fff); }
.mode-menu-item span { font-size: 0.72rem; opacity: 0.65; }
```

- [ ] **Step 5: ModeErrorBoundary + ModeLayer + 공용 CSS 구현**

`src/modes/ModeErrorBoundary.jsx`:

```jsx
import { Component } from 'react'

export default class ModeErrorBoundary extends Component {
  state = { failed: false }

  static getDerivedStateFromError() {
    return { failed: true }
  }

  componentDidCatch(err) {
    console.error('[labmode] mode crashed, returning to normal:', err)
    this.props.onError?.()
  }

  componentDidUpdate(prev) {
    if (prev.children !== this.props.children && this.state.failed) {
      this.setState({ failed: false })
    }
  }

  render() {
    return this.state.failed ? null : this.props.children
  }
}
```

`src/modes/ModeLayer.jsx`:

```jsx
import { Suspense, useEffect } from 'react'
import { useMode } from './ModeContext.jsx'
import { getMode } from './registry.js'
import ModeErrorBoundary from './ModeErrorBoundary.jsx'
import './modes.css'

export default function ModeLayer() {
  const { modeId, exitMode } = useMode()
  const mode = getMode(modeId)

  useEffect(() => {
    if (!mode) return
    const onKey = (e) => {
      if (e.key === 'Escape') exitMode()
    }
    window.addEventListener('keydown', onKey)
    return () => window.removeEventListener('keydown', onKey)
  }, [mode, exitMode])

  if (!mode) return null
  const Comp = mode.component
  return (
    <>
      <ModeErrorBoundary onError={exitMode}>
        <Suspense fallback={null}>
          <Comp />
        </Suspense>
      </ModeErrorBoundary>
      <div className="mode-badge" style={{ '--mode-color': mode.color }}>
        <span className="mode-badge-dot" aria-hidden="true" />
        {mode.title}
        <button type="button" className="mode-badge-exit" onClick={exitMode} aria-label="Exit mode (Esc)">
          ✕
        </button>
      </div>
    </>
  )
}
```

`src/modes/modes.css`:

```css
.mode-badge {
  position: fixed;
  top: 76px;
  right: 16px;
  z-index: 2100;
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 6px 10px 6px 12px;
  border-radius: 999px;
  background: rgba(12, 14, 22, 0.85);
  border: 1px solid rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  font-size: 0.75rem;
  letter-spacing: 0.06em;
  color: #fff;
}

.mode-badge-dot {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--mode-color, #7aa2ff);
}

.mode-badge-exit {
  border: 0;
  background: rgba(255, 255, 255, 0.1);
  color: #fff;
  width: 20px;
  height: 20px;
  border-radius: 50%;
  cursor: pointer;
  font-size: 0.7rem;
  line-height: 1;
}
```

- [ ] **Step 6: App.jsx 통합**

`src/App.jsx` — import 2줄 추가:

```jsx
import { ModeProvider } from './modes/ModeContext.jsx'
import ModeLayer from './modes/ModeLayer.jsx'
```

`AppContent`의 return을 다음처럼 수정 (기존 자식은 그대로, `ModeProvider`로 감싸고 `isMainPage`일 때 `ModeLayer`):

```jsx
return (
  <LangProvider>
    <ModeProvider>
      <SpaceBackground />
      <Cursor />
      <Navbar />
      <Routes>
        <Route path="/" element={<MainPage />} />
        <Route path="/gallery" element={<Gallery />} />
        <Route path="/gallery/:id" element={<ExperimentPage />} />
        <Route path="/gallery/:id/code" element={<CodePage />} />
        <Route path="/projects/:slug" element={<ProjectPage />} />
      </Routes>
      {isMainPage && <ModeLayer />}
      {showGlobalFooter && <Footer />}
    </ModeProvider>
  </LangProvider>
)
```

- [ ] **Step 7: Navbar 통합**

`src/components/Navbar/Navbar.jsx` — import 추가:

```jsx
import ModeMenu from '../../modes/ModeSelector/ModeMenu.jsx'
```

데스크톱 `<nav className="nav-links">` 안, `Lab` Link 바로 다음(nav-cta 앞)에 1줄:

```jsx
{location.pathname === '/' && <ModeMenu />}
```

- [ ] **Step 8: E2E 통과 확인**

Run: `npm run test:e2e`
Expected: PASS — 2 passed.

- [ ] **Step 9: 단위 테스트 회귀 + 커밋**

Run: `npm test` — Expected: PASS.

```bash
git add src/modes src/App.jsx src/components/Navbar/Navbar.jsx playwright.config.js e2e/mode-menu.spec.js package.json
git commit -m "feat(modes): add mode menu, mode layer and app integration with e2e setup

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: Terminal 모드

**Files:**
- Create: `src/data/projects.js` (Projects.jsx의 배열 이동)
- Modify: `src/sections/Projects/Projects.jsx` (배열 제거 + import)
- Create: `src/modes/Terminal/commands.js`
- Create: `src/modes/Terminal/commands.test.js`
- Create: `src/modes/Terminal/Terminal.jsx`
- Create: `src/modes/Terminal/Terminal.css`
- Modify: `src/modes/registry.js` (terminal 항목 추가)
- Create: `e2e/terminal.spec.js`

**Interfaces:**
- Consumes: `useMode().exitMode`, `useLang().t` (`t.about.body1`, `t.about.body2`, `t.skills.sub`), react-router `useNavigate`.
- Produces: `src/data/projects.js`가 `export const projects` (각 항목에 `slug`, `title`, `category` 필드 존재 — 기존 배열 그대로). `runCommand(rawInput, { projects, t }) → { output: string[], action?: { type: 'exit' | 'navigate' | 'clear', to?: string } }`.

- [ ] **Step 1: projects 데이터 추출**

`src/sections/Projects/Projects.jsx`에서 `const projects = [ ... ]` 블록(파일 상단, 첫 3개 항목이 FindX/Spotline/Lyralab)을 **그대로 잘라내** `src/data/projects.js`로 옮기고 `export const projects = [ ... ]`로 바꾼다. 내용은 한 글자도 수정하지 않는다 (작업 트리에 미커밋 변경이 있는 파일이므로 현재 상태 기준으로 이동).

`Projects.jsx`에는 import 1줄 추가:

```jsx
import { projects } from '../../data/projects.js'
```

검증: `npm run build` — Expected: exit 0. 그리고 `git diff src/sections/Projects/Projects.jsx`에 배열 삭제 + import 추가 외 다른 변경이 없어야 한다.

- [ ] **Step 2: 실패하는 파서 테스트 작성**

`src/modes/Terminal/commands.test.js`:

```js
import { describe, it, expect } from 'vitest'
import { runCommand } from './commands.js'

const ctx = {
  projects: [
    { slug: 'findx', title: 'FindX', category: 'Fullstack' },
    { slug: 'spotline', title: 'Spotline', category: 'AI · Fullstack' },
  ],
  t: {
    about: { body1: 'ABOUT_ONE', body2: 'ABOUT_TWO' },
    skills: { sub: 'SKILLS_SUB' },
  },
}

describe('runCommand', () => {
  it('help lists commands', () => {
    const res = runCommand('help', ctx)
    expect(res.output.join('\n')).toContain('ls')
    expect(res.output.join('\n')).toContain('exit')
  })

  it('empty input outputs nothing', () => {
    expect(runCommand('   ', ctx)).toEqual({ output: [] })
  })

  it('cat about.md prints about copy from ctx', () => {
    const res = runCommand('cat about.md', ctx)
    expect(res.output).toContain('ABOUT_ONE')
    expect(res.output).toContain('ABOUT_TWO')
  })

  it('projects lists slugs and titles', () => {
    const out = runCommand('projects', ctx).output.join('\n')
    expect(out).toContain('findx')
    expect(out).toContain('Spotline')
  })

  it('open <slug> returns navigate action; unknown slug errors', () => {
    expect(runCommand('open findx', ctx).action).toEqual({ type: 'navigate', to: '/projects/findx' })
    expect(runCommand('open nope', ctx).action).toBeUndefined()
  })

  it('exit and clear return actions', () => {
    expect(runCommand('exit', ctx).action).toEqual({ type: 'exit' })
    expect(runCommand('clear', ctx).action).toEqual({ type: 'clear' })
  })

  it('unknown command suggests help', () => {
    expect(runCommand('wat', ctx).output[0]).toContain("try 'help'")
  })

  it('sudo hire-me easter egg', () => {
    expect(runCommand('sudo hire-me', ctx).output.join(' ')).toContain('vmfhrmfoald36@gmail.com')
  })
})
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL — `commands.js` 로드 에러.

- [ ] **Step 4: 파서 구현**

`src/modes/Terminal/commands.js`:

```js
// 터미널 명령 해석기 — 순수 함수. UI 상태를 만지지 않는다.
export function runCommand(rawInput, ctx) {
  const input = rawInput.trim()
  if (!input) return { output: [] }
  const [cmd, ...args] = input.split(/\s+/)
  const arg = args.join(' ')
  const { projects, t } = ctx

  switch (cmd) {
    case 'help':
      return {
        output: [
          'Available commands:',
          '  ls              list portfolio files',
          '  cat about.md    who is Evan?',
          '  projects        list projects',
          '  open <slug>     open a project page',
          '  skills          tech stack',
          '  contact         how to reach me',
          '  clear           clear the screen',
          '  exit            back to the site',
        ],
      }
    case 'ls':
      return { output: ['about.md', 'projects/', 'skills.txt', 'contact.txt'] }
    case 'cat':
      if (arg === 'about.md') return { output: [t.about.body1, '', t.about.body2] }
      if (arg === 'skills.txt') return { output: [t.skills.sub] }
      if (arg === 'contact.txt') return contactOutput()
      return { output: [`cat: ${arg || '?'}: No such file`] }
    case 'projects':
      return { output: projects.map((p) => `${p.slug.padEnd(12)} ${p.title} — ${p.category}`) }
    case 'open': {
      const p = projects.find((x) => x.slug === arg)
      if (!p) return { output: [`open: ${arg || '?'}: not found (try 'projects')`] }
      return { output: [`Opening ${p.title}...`], action: { type: 'navigate', to: `/projects/${p.slug}` } }
    }
    case 'skills':
      return { output: [t.skills.sub] }
    case 'contact':
      return contactOutput()
    case 'clear':
      return { output: [], action: { type: 'clear' } }
    case 'exit':
      return { output: ['bye.'], action: { type: 'exit' } }
    case 'sudo':
      if (arg === 'hire-me') return { output: ['Permission granted. → vmfhrmfoald36@gmail.com'] }
      return { output: [`sudo: ${arg || '?'}: command not found`] }
    default:
      return { output: [`command not found: ${cmd} (try 'help')`] }
  }
}

function contactOutput() {
  return {
    output: [
      'email    : vmfhrmfoald36@gmail.com',
      'github   : github.com/Evvvaaaaan',
      'linkedin : linkedin.com/in/evvvaaaaan',
    ],
  }
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `npm test`
Expected: PASS — registry 3 + commands 8 = 11 passed.

- [ ] **Step 6: Terminal UI 구현**

`src/modes/Terminal/Terminal.jsx`:

```jsx
import { useEffect, useRef, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useLang } from '../../context/LangContext'
import { useMode } from '../ModeContext.jsx'
import { projects } from '../../data/projects.js'
import { runCommand } from './commands.js'
import './Terminal.css'

const BANNER = [
  'evan@portfolio — labmode/terminal',
  "type 'help' to get started, 'exit' to leave.",
  '',
]

export default function Terminal() {
  const { t } = useLang()
  const { exitMode } = useMode()
  const navigate = useNavigate()
  const [lines, setLines] = useState(BANNER)
  const [value, setValue] = useState('')
  const inputRef = useRef(null)
  const screenRef = useRef(null)

  useEffect(() => {
    inputRef.current?.focus()
  }, [])

  useEffect(() => {
    const el = screenRef.current
    if (el) el.scrollTop = el.scrollHeight
  }, [lines])

  const submit = (e) => {
    e.preventDefault()
    const res = runCommand(value, { projects, t })
    let next = [...lines, `$ ${value}`, ...res.output]
    if (res.action?.type === 'clear') next = []
    setLines(next)
    setValue('')
    if (res.action?.type === 'exit') exitMode()
    if (res.action?.type === 'navigate') {
      exitMode()
      navigate(res.action.to)
    }
  }

  return (
    <div
      className="terminal-overlay"
      role="dialog"
      aria-label="Terminal portfolio"
      onClick={() => inputRef.current?.focus()}
    >
      <div className="terminal-screen" ref={screenRef}>
        {lines.map((l, i) => (
          <pre key={i} className="terminal-line">{l || ' '}</pre>
        ))}
        <form className="terminal-input-row" onSubmit={submit}>
          <span className="terminal-prompt" aria-hidden="true">$</span>
          <input
            ref={inputRef}
            className="terminal-input"
            value={value}
            onChange={(e) => setValue(e.target.value)}
            spellCheck={false}
            autoComplete="off"
            aria-label="Terminal input"
          />
        </form>
      </div>
    </div>
  )
}
```

`src/modes/Terminal/Terminal.css`:

```css
.terminal-overlay {
  position: fixed;
  inset: 0;
  z-index: 2000;
  background: rgba(4, 8, 5, 0.97);
  display: flex;
  padding: 72px 16px 16px;
  cursor: text;
}

.terminal-screen {
  width: min(860px, 100%);
  margin: 0 auto;
  overflow-y: auto;
  font-family: 'SF Mono', ui-monospace, Menlo, monospace;
  font-size: 0.85rem;
  line-height: 1.55;
  color: #4af626;
}

.terminal-line {
  margin: 0;
  white-space: pre-wrap;
  word-break: break-word;
  font: inherit;
}

.terminal-input-row {
  display: flex;
  gap: 8px;
  align-items: center;
}

.terminal-prompt { opacity: 0.7; }

.terminal-input {
  flex: 1;
  border: 0;
  outline: 0;
  background: transparent;
  color: inherit;
  font: inherit;
  caret-color: #4af626;
}
```

- [ ] **Step 7: registry 등록**

`src/modes/registry.js` 상단에 import 추가, `modes` 배열에 항목 추가:

```js
import { lazy } from 'react'

export const modes = [
  {
    id: 'terminal',
    title: 'Terminal',
    description: 'Browse this portfolio like a CLI.',
    color: '#4af626',
    component: lazy(() => import('./Terminal/Terminal.jsx')),
  },
]
```

- [ ] **Step 8: E2E 작성 + 실행**

`e2e/terminal.spec.js`:

```js
import { test, expect } from '@playwright/test'

test('terminal mode: enter, run help, exit', async ({ page }) => {
  await page.goto('/')
  await page.getByRole('button', { name: 'Mode' }).click()
  await page.getByRole('menuitem', { name: /Terminal/ }).click()

  const input = page.getByLabel('Terminal input')
  await expect(input).toBeVisible()
  await input.fill('help')
  await input.press('Enter')
  await expect(page.getByText('Available commands:')).toBeVisible()

  await input.fill('exit')
  await input.press('Enter')
  await expect(page.locator('.terminal-overlay')).toHaveCount(0)
})

test('terminal mode: ESC exits', async ({ page }) => {
  await page.goto('/')
  await page.getByRole('button', { name: 'Mode' }).click()
  await page.getByRole('menuitem', { name: /Terminal/ }).click()
  await expect(page.locator('.terminal-overlay')).toBeVisible()
  await page.keyboard.press('Escape')
  await expect(page.locator('.terminal-overlay')).toHaveCount(0)
})
```

Run: `npm run test:e2e` — Expected: PASS (기존 2 + 신규 2).
Run: `npm test` — Expected: PASS.

- [ ] **Step 9: Commit**

```bash
git add src/data/projects.js src/sections/Projects/Projects.jsx src/modes/Terminal src/modes/registry.js e2e/terminal.spec.js
git commit -m "feat(modes): add Terminal mode with command parser

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

주의: `Projects.jsx`에 사용자 미커밋 변경이 섞여 있으므로 `git diff --staged`로 배열 이동 + import 외의 변경이 스테이징되지 않았는지 확인 후 커밋. 무관 변경이 섞이면 `git add -p`로 분리.

---

### Task 4: Speedrun 모드

**Files:**
- Create: `src/modes/Speedrun/splits.js`
- Create: `src/modes/Speedrun/splits.test.js`
- Create: `src/modes/Speedrun/Speedrun.jsx`
- Create: `src/modes/Speedrun/Speedrun.css`
- Modify: `src/modes/registry.js` (speedrun 항목 추가)
- Create: `e2e/speedrun.spec.js`

**Interfaces:**
- Consumes: 섹션 앵커 `#home #about #projects #skills #contact` (실존 확인됨).
- Produces: `MISSIONS: [{ id, label, selector }]`, `createRun(now) → run`, `completeMission(run, id, now) → run` (불변, 전부 완료 시 `finishedAt` 설정), `formatMs(ms) → 'm:ss.cc'`, `loadBest() → number|null`, `saveBest(ms) → boolean`.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/modes/Speedrun/splits.test.js`:

```js
import { describe, it, expect, beforeEach } from 'vitest'
import { MISSIONS, createRun, completeMission, formatMs, loadBest, saveBest } from './splits.js'

// node 환경에는 localStorage가 없으므로 스텁
beforeEach(() => {
  const store = new Map()
  globalThis.localStorage = {
    getItem: (k) => (store.has(k) ? store.get(k) : null),
    setItem: (k, v) => store.set(k, String(v)),
  }
})

describe('splits engine', () => {
  it('completes missions and finishes when all are done', () => {
    let run = createRun(1000)
    for (const m of MISSIONS.slice(0, -1)) {
      run = completeMission(run, m.id, 2000)
      expect(run.finishedAt).toBeNull()
    }
    run = completeMission(run, MISSIONS.at(-1).id, 5000)
    expect(run.finishedAt).toBe(5000)
    expect(run.splits[MISSIONS.at(-1).id]).toBe(4000)
  })

  it('is idempotent per mission and frozen after finish', () => {
    let run = createRun(0)
    run = completeMission(run, MISSIONS[0].id, 100)
    const again = completeMission(run, MISSIONS[0].id, 999)
    expect(again).toBe(run)
  })

  it('formatMs renders m:ss.cc', () => {
    expect(formatMs(0)).toBe('0:00.00')
    expect(formatMs(83_456)).toBe('1:23.45')
  })

  it('saveBest keeps the minimum', () => {
    expect(loadBest()).toBeNull()
    expect(saveBest(5000)).toBe(true)
    expect(saveBest(7000)).toBe(false)
    expect(loadBest()).toBe(5000)
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL — `splits.js` 로드 에러.

- [ ] **Step 3: splits 엔진 구현**

`src/modes/Speedrun/splits.js`:

```js
export const MISSIONS = [
  { id: 'home', label: 'Reach Hero', selector: '#home' },
  { id: 'about', label: 'Visit About', selector: '#about' },
  { id: 'projects', label: 'Visit Projects', selector: '#projects' },
  { id: 'skills', label: 'Visit Skills', selector: '#skills' },
  { id: 'contact', label: 'Visit Contact', selector: '#contact' },
]

export function createRun(now) {
  return { startedAt: now, splits: {}, finishedAt: null }
}

export function completeMission(run, id, now) {
  if (run.finishedAt != null || run.splits[id] != null) return run
  const splits = { ...run.splits, [id]: now - run.startedAt }
  const finished = MISSIONS.every((m) => splits[m.id] != null)
  return { ...run, splits, finishedAt: finished ? now : null }
}

export function formatMs(ms) {
  const m = Math.floor(ms / 60000)
  const s = Math.floor((ms % 60000) / 1000)
  const cs = Math.floor((ms % 1000) / 10)
  return `${m}:${String(s).padStart(2, '0')}.${String(cs).padStart(2, '0')}`
}

const BEST_KEY = 'labmode.speedrun.best'

export function loadBest() {
  const v = Number(localStorage.getItem(BEST_KEY))
  return Number.isFinite(v) && v > 0 ? v : null
}

export function saveBest(ms) {
  const best = loadBest()
  if (best == null || ms < best) {
    localStorage.setItem(BEST_KEY, String(ms))
    return true
  }
  return false
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS — 누적 15 passed.

- [ ] **Step 5: Speedrun UI 구현**

`src/modes/Speedrun/Speedrun.jsx`:

```jsx
import { useEffect, useRef, useState } from 'react'
import { MISSIONS, createRun, completeMission, formatMs, loadBest, saveBest } from './splits.js'
import './Speedrun.css'

export default function Speedrun() {
  const [run, setRun] = useState(() => createRun(performance.now()))
  const [best, setBest] = useState(loadBest)
  const timerRef = useRef(null)
  const runRef = useRef(run)
  runRef.current = run

  // 타이머는 rAF로 DOM에 직접 쓴다 — 프레임마다 리렌더하지 않기 위함
  useEffect(() => {
    let id
    const loop = () => {
      const r = runRef.current
      const ms = (r.finishedAt ?? performance.now()) - r.startedAt
      if (timerRef.current) timerRef.current.textContent = formatMs(ms)
      id = requestAnimationFrame(loop)
    }
    id = requestAnimationFrame(loop)
    return () => cancelAnimationFrame(id)
  }, [])

  useEffect(() => {
    const targets = MISSIONS
      .map((m) => [m, document.querySelector(m.selector)])
      .filter(([, el]) => el)
    const io = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (!entry.isIntersecting) continue
          const hit = targets.find(([, el]) => el === entry.target)
          if (hit) setRun((r) => completeMission(r, hit[0].id, performance.now()))
        }
      },
      { threshold: 0.4 },
    )
    targets.forEach(([, el]) => io.observe(el))
    return () => io.disconnect()
  }, [])

  useEffect(() => {
    if (run.finishedAt == null) return
    const total = run.finishedAt - run.startedAt
    if (saveBest(total)) setBest(total)
  }, [run.finishedAt, run.startedAt])

  const restart = () => {
    window.scrollTo({ top: 0 })
    setRun(createRun(performance.now()))
  }

  return (
    <aside className="speedrun-panel" aria-label="Speedrun timer">
      <div className="speedrun-timer" ref={timerRef}>0:00.00</div>
      <ol className="speedrun-splits">
        {MISSIONS.map((m) => (
          <li key={m.id} className={run.splits[m.id] != null ? 'speedrun-split--done' : ''}>
            <span>{m.label}</span>
            <span>{run.splits[m.id] != null ? formatMs(run.splits[m.id]) : '—'}</span>
          </li>
        ))}
      </ol>
      <div className="speedrun-footer">
        {run.finishedAt != null && <strong className="speedrun-clear">CLEAR!</strong>}
        <span>best {best != null ? formatMs(best) : '—'}</span>
        <button type="button" onClick={restart}>restart</button>
      </div>
    </aside>
  )
}
```

`src/modes/Speedrun/Speedrun.css`:

```css
.speedrun-panel {
  position: fixed;
  top: 120px;
  right: 16px;
  z-index: 2000;
  width: 220px;
  padding: 12px;
  border-radius: 12px;
  background: rgba(12, 14, 22, 0.88);
  border: 1px solid rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  color: #fff;
  font-family: 'SF Mono', ui-monospace, Menlo, monospace;
  font-size: 0.72rem;
}

.speedrun-timer {
  font-size: 1.4rem;
  text-align: right;
  font-variant-numeric: tabular-nums;
  margin-bottom: 8px;
}

.speedrun-splits {
  list-style: none;
  margin: 0 0 8px;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.speedrun-splits li {
  display: flex;
  justify-content: space-between;
  opacity: 0.5;
  font-variant-numeric: tabular-nums;
}

.speedrun-split--done { opacity: 1; color: #ffd75e; }

.speedrun-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 6px;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  padding-top: 8px;
}

.speedrun-clear { color: #ffd75e; }

.speedrun-footer button {
  border: 1px solid rgba(255, 255, 255, 0.2);
  background: transparent;
  color: inherit;
  font: inherit;
  border-radius: 6px;
  padding: 2px 8px;
  cursor: pointer;
}
```

- [ ] **Step 6: registry 등록**

`src/modes/registry.js`의 `modes` 배열에 추가:

```js
{
  id: 'speedrun',
  title: 'Speedrun',
  description: 'Clear every section against the clock.',
  color: '#ffd75e',
  component: lazy(() => import('./Speedrun/Speedrun.jsx')),
},
```

- [ ] **Step 7: E2E 작성 + 실행**

`e2e/speedrun.spec.js`:

```js
import { test, expect } from '@playwright/test'

test('speedrun: timer runs and scrolling to bottom clears all splits', async ({ page }) => {
  await page.goto('/')
  await page.getByRole('button', { name: 'Mode' }).click()
  await page.getByRole('menuitem', { name: /Speedrun/ }).click()

  await expect(page.locator('.speedrun-panel')).toBeVisible()
  await page.waitForTimeout(300)
  await expect(page.locator('.speedrun-timer')).not.toHaveText('0:00.00')

  for (const id of ['#about', '#projects', '#skills', '#contact']) {
    await page.locator(id).scrollIntoViewIfNeeded()
    await page.waitForTimeout(400)
  }
  await expect(page.locator('.speedrun-split--done')).toHaveCount(5)
  await expect(page.locator('.speedrun-clear')).toBeVisible()
})
```

Run: `npm run test:e2e` — Expected: PASS (누적 5).
Run: `npm test` — Expected: PASS.

주: Lenis 스무스 스크롤 때문에 `scrollIntoViewIfNeeded` 후 IntersectionObserver 발화까지 짧은 대기가 필요하다. 400ms로 불안정하면 `await expect(...).toHaveCount(n)`의 자동 재시도에 맡기도록 각 스크롤 뒤 단언을 옮길 것.

- [ ] **Step 8: Commit**

```bash
git add src/modes/Speedrun src/modes/registry.js e2e/speedrun.spec.js
git commit -m "feat(modes): add Speedrun mode with section splits

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: Destruction 모드

**Files:**
- Create: `src/modes/Destruction/snapshot.js`
- Create: `src/modes/Destruction/snapshot.test.js`
- Create: `src/modes/Destruction/Destruction.jsx`
- Create: `src/modes/Destruction/Destruction.css`
- Modify: `src/modes/registry.js` (destruction 항목 추가)
- Create: `e2e/destruction.spec.js`

**Interfaces:**
- Consumes: `matter-js`(기존 의존성), `getLenis()` (`src/hooks/useLenis.js`의 기존 export — Lenis 인스턴스 `.stop()`/`.start()` 지원), `useMode().exitMode`.
- Produces: `collectTargets(doc, viewportH) → [{ el, rect: { x, y, w, h } }]` (가시 요소, 면적 내림차순, 최대 `MAX_BODIES`), `MAX_BODIES = 80`.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/modes/Destruction/snapshot.test.js`:

```js
import { describe, it, expect } from 'vitest'
import { collectTargets, MAX_BODIES } from './snapshot.js'

function fakeEl(x, y, w, h) {
  return {
    getBoundingClientRect: () => ({ left: x, top: y, right: x + w, bottom: y + h, width: w, height: h }),
    contains: () => false,
  }
}

const doc = (els) => ({ querySelectorAll: () => els })

describe('collectTargets', () => {
  it('filters tiny and off-screen elements', () => {
    const els = [
      fakeEl(0, 10, 100, 40),      // 화면 안 — 포함
      fakeEl(0, 10, 4, 4),         // 너무 작음 — 제외
      fakeEl(0, 2000, 100, 40),    // 뷰포트 아래 — 제외
      fakeEl(0, -100, 100, 40),    // 뷰포트 위 — 제외
    ]
    const out = collectTargets(doc(els), 800)
    expect(out).toHaveLength(1)
    expect(out[0].rect).toEqual({ x: 0, y: 10, w: 100, h: 40 })
  })

  it('skips children of already-collected elements', () => {
    const parent = fakeEl(0, 0, 300, 300)
    const child = fakeEl(10, 10, 50, 50)
    parent.contains = (el) => el === child
    const out = collectTargets(doc([parent, child]), 800)
    expect(out).toHaveLength(1)
  })

  it('caps at MAX_BODIES, keeping largest first', () => {
    const els = Array.from({ length: MAX_BODIES + 20 }, (_, i) => fakeEl(0, 1, 10 + i, 10))
    const out = collectTargets(doc(els), 800)
    expect(out).toHaveLength(MAX_BODIES)
    expect(out[0].rect.w).toBeGreaterThan(out.at(-1).rect.w)
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL — `snapshot.js` 로드 에러.

- [ ] **Step 3: snapshot 구현**

`src/modes/Destruction/snapshot.js`:

```js
const CANDIDATE_SELECTOR = [
  '#home h1', '#home p', '#home a', '#home button',
  '#about h2', '#about p',
  '#projects h2', '#projects .project-card',
  '#skills h2', '#skills li',
  '#contact h2', '#contact p', '#contact form',
].join(', ')

export const MAX_BODIES = 80

// 뷰포트 안에 보이는 요소만 골라 큰 것부터 최대 MAX_BODIES개 반환
export function collectTargets(doc = document, viewportH = window.innerHeight) {
  const els = [...doc.querySelectorAll(CANDIDATE_SELECTOR)]
  const picked = []
  for (const el of els) {
    if (picked.some((p) => p.el.contains(el))) continue
    const r = el.getBoundingClientRect()
    if (r.width < 8 || r.height < 8) continue
    if (r.bottom <= 0 || r.top >= viewportH) continue
    picked.push({ el, rect: { x: r.left, y: r.top, w: r.width, h: r.height } })
  }
  return picked
    .sort((a, b) => b.rect.w * b.rect.h - a.rect.w * a.rect.h)
    .slice(0, MAX_BODIES)
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS — 누적 18 passed.

- [ ] **Step 5: Destruction 컴포넌트 구현**

`src/modes/Destruction/Destruction.jsx`:

```jsx
import { useEffect, useRef } from 'react'
import Matter from 'matter-js'
import { useMode } from '../ModeContext.jsx'
import { getLenis } from '../../hooks/useLenis'
import { collectTargets } from './snapshot.js'
import './Destruction.css'

export default function Destruction() {
  const { exitMode } = useMode()
  const hostRef = useRef(null)

  useEffect(() => {
    const targets = collectTargets()
    if (targets.length === 0) {
      exitMode()
      return
    }

    const host = hostRef.current
    const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches
    const lenis = getLenis()
    lenis?.stop()
    const prevOverflow = document.body.style.overflow
    document.body.style.overflow = 'hidden'

    // 원본을 숨기고 시각적 클론을 물리 바디와 1:1로 만든다
    const items = targets.map(({ el, rect }) => {
      const clone = el.cloneNode(true)
      clone.setAttribute('aria-hidden', 'true')
      Object.assign(clone.style, {
        position: 'fixed',
        left: '0',
        top: '0',
        margin: '0',
        width: `${rect.w}px`,
        height: `${rect.h}px`,
        transform: `translate(${rect.x}px, ${rect.y}px)`,
        willChange: 'transform',
        pointerEvents: 'none',
        overflow: 'hidden',
      })
      host.appendChild(clone)
      el.dataset.labmodePrevVisibility = el.style.visibility || ''
      el.style.visibility = 'hidden'
      const body = Matter.Bodies.rectangle(
        rect.x + rect.w / 2, rect.y + rect.h / 2, rect.w, rect.h,
        { restitution: 0.4, friction: 0.6 },
      )
      return { el, clone, body, w: rect.w, h: rect.h }
    })

    const engine = Matter.Engine.create()
    engine.gravity.y = reduced ? 0.25 : 1
    const W = window.innerWidth
    const H = window.innerHeight
    const T = 200
    const walls = [
      Matter.Bodies.rectangle(W / 2, H + T / 2, W * 2, T, { isStatic: true }),
      Matter.Bodies.rectangle(-T / 2, H / 2, T, H * 4, { isStatic: true }),
      Matter.Bodies.rectangle(W + T / 2, H / 2, T, H * 4, { isStatic: true }),
    ]
    Matter.Composite.add(engine.world, [...walls, ...items.map((i) => i.body)])

    const mouse = Matter.Mouse.create(host)
    const mouseConstraint = Matter.MouseConstraint.create(engine, {
      mouse,
      constraint: { stiffness: 0.15 },
    })
    Matter.Composite.add(engine.world, mouseConstraint)

    let raf
    let last = performance.now()
    const loop = (now) => {
      raf = requestAnimationFrame(loop)
      const dt = Math.min(now - last, 33)
      last = now
      Matter.Engine.update(engine, dt)
      for (const { clone, body, w, h } of items) {
        clone.style.transform =
          `translate(${body.position.x - w / 2}px, ${body.position.y - h / 2}px) rotate(${body.angle}rad)`
      }
    }
    raf = requestAnimationFrame(loop)

    return () => {
      cancelAnimationFrame(raf)
      Matter.Composite.clear(engine.world, false)
      Matter.Engine.clear(engine)
      for (const { el, clone } of items) {
        clone.remove()
        el.style.visibility = el.dataset.labmodePrevVisibility
        delete el.dataset.labmodePrevVisibility
      }
      document.body.style.overflow = prevOverflow
      lenis?.start()
    }
  }, [exitMode])

  return (
    <div className="destruction-host" ref={hostRef}>
      <button type="button" className="destruction-restore" onClick={exitMode}>
        RESTORE
      </button>
    </div>
  )
}
```

`src/modes/Destruction/Destruction.css`:

```css
.destruction-host {
  position: fixed;
  inset: 0;
  z-index: 2000;
  cursor: grab;
}

.destruction-host:active { cursor: grabbing; }

.destruction-restore {
  position: fixed;
  bottom: 24px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 2100;
  padding: 10px 22px;
  border-radius: 999px;
  border: 1px solid rgba(255, 255, 255, 0.25);
  background: rgba(12, 14, 22, 0.9);
  color: #ff6b6b;
  font-family: 'SF Mono', ui-monospace, Menlo, monospace;
  font-size: 0.8rem;
  letter-spacing: 0.12em;
  cursor: pointer;
}
```

- [ ] **Step 6: registry 등록**

`src/modes/registry.js`의 `modes` 배열에 추가:

```js
{
  id: 'destruction',
  title: 'Destruction',
  description: 'Watch the page collapse. Throw the debris.',
  color: '#ff6b6b',
  component: lazy(() => import('./Destruction/Destruction.jsx')),
},
```

- [ ] **Step 7: E2E 작성 + 실행**

`e2e/destruction.spec.js`:

```js
import { test, expect } from '@playwright/test'

test('destruction: page elements collapse and RESTORE brings them back', async ({ page }) => {
  await page.goto('/')
  await page.getByRole('button', { name: 'Mode' }).click()
  await page.getByRole('menuitem', { name: /Destruction/ }).click()

  const host = page.locator('.destruction-host')
  await expect(host).toBeVisible()
  // 클론이 생성되었는지 (RESTORE 버튼 외 자식 존재)
  expect(await host.locator('[aria-hidden="true"]').count()).toBeGreaterThan(3)
  // 원본 히어로 제목은 숨겨짐
  await expect(page.locator('#home h1').first()).toBeHidden()

  await page.getByRole('button', { name: 'RESTORE' }).click()
  await expect(host).toHaveCount(0)
  await expect(page.locator('#home h1').first()).toBeVisible()
})
```

Run: `npm run test:e2e` — Expected: PASS (누적 6).
Run: `npm test` — Expected: PASS.

주: `#home h1`이 없다면(히어로 제목이 3D 캔버스라면) `#home p`로 대체하고 스텝을 갱신할 것 — 단언 대상은 "collectTargets가 실제로 잡은 요소"여야 한다.

- [ ] **Step 8: Commit**

```bash
git add src/modes/Destruction src/modes/registry.js e2e/destruction.spec.js
git commit -m "feat(modes): add Destruction mode with matter-js physics

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: Inspect 모드

**Files:**
- Create: `src/modes/Inspect/annotations.js`
- Create: `src/modes/Inspect/Inspect.jsx`
- Create: `src/modes/Inspect/Inspect.css`
- Modify: `src/modes/registry.js` (inspect 항목 추가)
- Create: `e2e/inspect.spec.js`

**Interfaces:**
- Consumes: 섹션 앵커 + `.navbar` (실존 확인됨), `useMode().exitMode`.
- Produces: `ANNOTATIONS: [{ selector, tag, title, note }]` — 투어 순서대로.

- [ ] **Step 1: annotations 데이터 작성**

`src/modes/Inspect/annotations.js`:

```js
// Inspect 투어 스텝. selector는 메인 페이지에 실존하는 요소여야 한다.
export const ANNOTATIONS = [
  {
    selector: '#home',
    tag: '<section id="home">',
    title: 'Hero',
    note: 'The opening scene. The title is rendered as real 3D geometry with three.js, and the role line rotates through what I do. First impressions are a rendering problem.',
  },
  {
    selector: '.navbar',
    tag: '<header class="navbar">',
    title: 'Navbar',
    note: 'Sticky, translucent, and language-aware. The language switcher is a keyboard-accessible radiogroup — arrow keys work. The Mode menu you just used lives here too.',
  },
  {
    selector: '#about',
    tag: '<section id="about">',
    title: 'About',
    note: 'Copy lives in a four-language i18n dictionary (en/ko/ja/zh), not in the markup. Scroll-reveal animations are driven by a small IntersectionObserver hook, not a library.',
  },
  {
    selector: '#projects',
    tag: '<section id="projects">',
    title: 'Projects',
    note: 'A horizontally scrolling card rail. Each card carries its own accent color, and project data is a single source shared with the Terminal mode you can try from the menu.',
  },
  {
    selector: '#skills',
    tag: '<section id="skills">',
    title: 'Skills',
    note: 'The stack I actually ship with. Rendered as a plain list on purpose — content first, decoration second.',
  },
  {
    selector: '#contact',
    tag: '<section id="contact">',
    title: 'Contact',
    note: 'A real form, real validation, real inbox. If this tour convinced you, this is the section that matters.',
  },
]
```

- [ ] **Step 2: 실패하는 E2E 작성**

`e2e/inspect.spec.js`:

```js
import { test, expect } from '@playwright/test'

test('inspect: tour panel steps through annotated elements', async ({ page }) => {
  await page.goto('/')
  await page.getByRole('button', { name: 'Mode' }).click()
  await page.getByRole('menuitem', { name: /Inspect/ }).click()

  const panel = page.locator('.inspect-panel')
  await expect(panel).toBeVisible()
  await expect(panel).toContainText('Hero')
  await expect(page.locator('.inspect-highlight')).toBeVisible()

  await page.getByRole('button', { name: 'Next' }).click()
  await expect(panel).toContainText('Navbar')

  await page.getByRole('button', { name: 'Prev' }).click()
  await expect(panel).toContainText('Hero')

  await page.keyboard.press('Escape')
  await expect(panel).toHaveCount(0)
})
```

- [ ] **Step 3: E2E 실패 확인**

Run: `npm run test:e2e -- inspect`
Expected: FAIL — Inspect menuitem 없음.

- [ ] **Step 4: Inspect 컴포넌트 구현**

`src/modes/Inspect/Inspect.jsx`:

```jsx
import { useEffect, useState } from 'react'
import { useMode } from '../ModeContext.jsx'
import { ANNOTATIONS } from './annotations.js'
import './Inspect.css'

export default function Inspect() {
  const { exitMode } = useMode()
  const [idx, setIdx] = useState(0)
  const [box, setBox] = useState(null)
  const current = ANNOTATIONS[idx]

  // 현재 스텝 대상으로 스크롤하고, 하이라이트 박스를 요소 위치에 고정
  useEffect(() => {
    const el = document.querySelector(current.selector)
    if (!el) {
      setBox(null)
      return
    }
    el.scrollIntoView({ behavior: 'smooth', block: 'center' })
    let raf
    const update = () => {
      const r = el.getBoundingClientRect()
      setBox({ x: r.left, y: r.top, w: r.width, h: r.height })
    }
    const schedule = () => {
      cancelAnimationFrame(raf)
      raf = requestAnimationFrame(update)
    }
    update()
    window.addEventListener('scroll', schedule, { passive: true })
    window.addEventListener('resize', schedule)
    return () => {
      cancelAnimationFrame(raf)
      window.removeEventListener('scroll', schedule)
      window.removeEventListener('resize', schedule)
    }
  }, [current])

  return (
    <>
      {box && (
        <div
          className="inspect-highlight"
          style={{
            transform: `translate(${box.x}px, ${box.y}px)`,
            width: `${box.w}px`,
            height: `${box.h}px`,
          }}
          aria-hidden="true"
        />
      )}
      <aside className="inspect-panel" aria-label="Inspect tour">
        <div className="inspect-tag">{current.tag}</div>
        <h3 className="inspect-title">{current.title}</h3>
        <p className="inspect-note">{current.note}</p>
        <div className="inspect-controls">
          <span className="inspect-step">{idx + 1} / {ANNOTATIONS.length}</span>
          <button type="button" disabled={idx === 0} onClick={() => setIdx((i) => i - 1)}>
            Prev
          </button>
          <button type="button" disabled={idx === ANNOTATIONS.length - 1} onClick={() => setIdx((i) => i + 1)}>
            Next
          </button>
          <button type="button" onClick={exitMode}>Exit</button>
        </div>
      </aside>
    </>
  )
}
```

`src/modes/Inspect/Inspect.css`:

```css
.inspect-highlight {
  position: fixed;
  left: 0;
  top: 0;
  z-index: 1900;
  pointer-events: none;
  outline: 2px solid #61dafb;
  outline-offset: -2px;
  background: rgba(97, 218, 251, 0.08);
  box-shadow: 0 0 0 9999px rgba(0, 0, 0, 0.45);
  transition: transform 0.2s ease, width 0.2s ease, height 0.2s ease;
}

.inspect-panel {
  position: fixed;
  top: 120px;
  right: 16px;
  z-index: 2000;
  width: 300px;
  padding: 14px;
  border-radius: 12px;
  background: rgba(12, 14, 22, 0.92);
  border: 1px solid rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  color: #fff;
}

.inspect-tag {
  font-family: 'SF Mono', ui-monospace, Menlo, monospace;
  font-size: 0.72rem;
  color: #61dafb;
  margin-bottom: 6px;
}

.inspect-title {
  margin: 0 0 6px;
  font-size: 1rem;
}

.inspect-note {
  margin: 0 0 12px;
  font-size: 0.78rem;
  line-height: 1.55;
  opacity: 0.8;
}

.inspect-controls {
  display: flex;
  align-items: center;
  gap: 6px;
}

.inspect-step {
  margin-right: auto;
  font-size: 0.72rem;
  opacity: 0.6;
  font-variant-numeric: tabular-nums;
}

.inspect-controls button {
  border: 1px solid rgba(255, 255, 255, 0.2);
  background: transparent;
  color: inherit;
  font: inherit;
  font-size: 0.75rem;
  border-radius: 6px;
  padding: 3px 10px;
  cursor: pointer;
}

.inspect-controls button:disabled {
  opacity: 0.35;
  cursor: default;
}
```

- [ ] **Step 5: registry 등록**

`src/modes/registry.js`의 `modes` 배열에 추가:

```js
{
  id: 'inspect',
  title: 'Inspect',
  description: 'A guided teardown of how this site is built.',
  color: '#61dafb',
  component: lazy(() => import('./Inspect/Inspect.jsx')),
},
```

- [ ] **Step 6: E2E 통과 확인**

Run: `npm run test:e2e` — Expected: PASS (누적 7).
Run: `npm test` — Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add src/modes/Inspect src/modes/registry.js e2e/inspect.spec.js
git commit -m "feat(modes): add Inspect mode with guided teardown tour

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: 최종 검증

**Files:** 수정 없음 (발견된 결함 수정만).

- [ ] **Step 1: 전체 검증 실행**

```bash
npm test          # Expected: 18 passed
npm run test:e2e  # Expected: 7 passed
npm run lint      # Expected: exit 0
npm run build     # Expected: exit 0, 모드 청크가 별도 파일로 분리되어 있는지 출력에서 확인
```

- [ ] **Step 2: 핵심 제약 수동 확인 (브라우저)**

`npm run dev` 후 확인:
1. normal 상태에서 네트워크 탭에 모드 청크(Terminal/Speedrun/Destruction/Inspect)가 로드되지 않음.
2. 각 모드 진입 → ESC → 페이지가 완전히 원상 복구됨 (특히 Destruction 후 텍스트 visibility).
3. 모드 뱃지·패널이 히어로/프로젝트 콘텐츠를 가리지 않음.

- [ ] **Step 3: 실패 발견 시**

실패한 항목의 태스크로 돌아가 수정 후 이 태스크를 다시 실행. 전부 통과하면 종료.
